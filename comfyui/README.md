# comfyui

阶段 3、4 才用到。把 ComfyUI 的工作流导出成 `.json` 存到 `workflows/`,便于复用和版本管理。

- `workflows/lipsync_musetalk.json` —— 对口型(阶段3)
- `workflows/bodyswap_wan22.json` —— 全身替换(阶段4)

模型权重不入 git,装在 ComfyUI 自己的 `models/` 下。
